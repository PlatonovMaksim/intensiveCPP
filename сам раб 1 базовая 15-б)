// Сам раб 1 базовая 15-б)

#include <iostream>
#include<math.h>
using namespace std;

int main()
{
    float x, y; 
    cout << "Введите x" << endl;
    cin >> x;
    y= sin((3.2 + sqrt(1+x))/ abs(5*x));
    cout << "Ответ: " << y << endl;
    return 0;
}
